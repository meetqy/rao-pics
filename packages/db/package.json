{
  "name": "@rao-pics/db",
  "version": "0.2.0",
  "main": "./index.ts",
  "scripts": {
    "postinstall": "pnpm db:reset",
    "db:push": "prisma db push",
    "db:reset": "pnpm db:push --force-reset",
    "test": "vitest",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "clean": "git clean -xdf node_modules prisma/db* .turbo"
  },
  "devDependencies": {
    "@types/node": "^20.4.9",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "prisma": "5.1.1",
    "@prisma/client": "5.1.1"
  }
}
