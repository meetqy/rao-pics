# 结构

- @eagleuse/
  - transform                   # 数据转换为sqlite
  - transform-client            # core的可视化页面
  - api                         # sqlite to api
  - theme-default               # theme-{name}
